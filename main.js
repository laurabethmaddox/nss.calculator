const subtract = (firstNumber, secondNumber) => {
    const difference = firstNumber - secondNumber
    return difference
}

let theDifference = subtract()
console.log(theDifference)

const divide = (firstNumber, secondNumber) => {
    const quotient = firstNumber / secondNumber
    return quotient
}

let theQuotient = divide()
console.log(theQuotient)

const multiply = (firstNumber, secondNumber) => {
    const product = firstNumber * secondNumber
    return product
}

let theProduct = multiply()
console.log(theProduct)

const square = (originalNumber) => {
    const squared = originalNumber * originalNumber

    return squared
}

let theSquared = square()
console.log(theSquared)